extends layout

block content
  .container
    form.form-signin(role='form', action='/search/save', method='post')
      h2.form-signin-heading Create Search
      label.sr-only(for='name')
      input.form-control(type='text', name='name', id='name', placeholder='Search Name', required, autofocus)
      input.form-control(type='url', name='url[]', id='url', placeholder='Url')
      input.form-control(type='url', name='url[]', id='url', placeholder='Url')
      input.form-control(type='url', name='url[]', id='url', placeholder='Url')
      button.btn.btn-lg.btn-primary.btn-block(type='submit') Save
